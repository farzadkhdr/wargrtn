# لە /etc/apache2/conf-available/cgi.conf
<Directory "/usr/lib/cgi-bin">
    AllowOverride None
    Options +ExecCGI -MultiViews +SymLinksIfOwnerMatch
    Require all granted
    AddHandler cgi-script .cgi
</Directory>

# چالاککردنی مۆدی CGI
sudo a2enmod cgi
sudo systemctl restart apache2
